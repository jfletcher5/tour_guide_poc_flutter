import '/components/tour_list/tour_list_widget.dart';
import '/flutter_flow/flutter_flow_theme.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'package:flutter/material.dart';
import 'componenttest_model.dart';
export 'componenttest_model.dart';

class ComponenttestWidget extends StatefulWidget {
  const ComponenttestWidget({super.key});

  @override
  State<ComponenttestWidget> createState() => _ComponenttestWidgetState();
}

class _ComponenttestWidgetState extends State<ComponenttestWidget> {
  late ComponenttestModel _model;

  final scaffoldKey = GlobalKey<ScaffoldState>();

  @override
  void initState() {
    super.initState();
    _model = createModel(context, () => ComponenttestModel());

    WidgetsBinding.instance.addPostFrameCallback((_) => setState(() {}));
  }

  @override
  void dispose() {
    _model.dispose();

    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: () => _model.unfocusNode.canRequestFocus
          ? FocusScope.of(context).requestFocus(_model.unfocusNode)
          : FocusScope.of(context).unfocus(),
      child: Scaffold(
        key: scaffoldKey,
        backgroundColor: FlutterFlowTheme.of(context).primaryBackground,
        body: SafeArea(
          top: true,
          child: Column(
            mainAxisSize: MainAxisSize.max,
            children: [
              Expanded(
                child: Container(
                  width: double.infinity,
                  height: 100.0,
                  decoration: BoxDecoration(
                    color: FlutterFlowTheme.of(context).secondaryBackground,
                  ),
                  child: wrapWithModel(
                    model: _model.tourListModel,
                    updateCallback: () => setState(() {}),
                    child: const TourListWidget(),
                  ),
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
